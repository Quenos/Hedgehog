const state = {
    exchange: 'deribit'
}

const getters = {
    getExchange: () => state.exchange
}

const actions = {}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}